# Forest Rover - playground repo

Code structure:

- `fe`: Frontend, include documents specific for the FE.
- `be`: Backend, not sure which technology to use yet.
- `ext`: built-in, bundled (?) extensions, not sure how to do that yet.
