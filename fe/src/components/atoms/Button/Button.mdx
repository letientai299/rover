# Button

import { FaDeleteLeft } from 'react-icons/fa6';
import Button from './Button.tsx';
import { FiFilePlus, FiFolder, FiPhoneCall, FiSun } from 'react-icons/fi';

export function Bar(props) {
  return (
    <div
      style={{
        display: 'flex',
        gap: `var(--gap-lg)`,
        margin: `var(--margin-lg)`,
      }}
    >
      {props.children}
    </div>
  );
}

## Basic

Icon at `start` position:

<Button icon={FiSun} variant={'outline'}>
  <>Toggle theme</>
</Button>

Icon at `end` position:

<Button icon={FiSun} iconPosition={'end'} variant={'outline'}>
  <>Toggle theme</>
</Button>

Icon only:

<Button icon={FiSun} variant={'outline'} />

## Disabled

<Bar>
  <Button icon={FiFilePlus} disabled kind={'normal'} variant={'ghost'}>
    <>Disabled</>
  </Button>
  <Button icon={FiFilePlus} kind={'normal'} variant={'ghost'}>
    <>Normal</>
  </Button>
  <Button icon={FiFilePlus} disabled kind={'danger'} variant={'ghost'}>
    <>Disabled</>
  </Button>
  <Button icon={FiFilePlus} kind={'danger'} variant={'ghost'}>
    <>Danger</>
  </Button>
  <Button icon={FiFilePlus} disabled kind={'primary'} variant={'ghost'}>
    <>Disabled</>
  </Button>
  <Button icon={FiFilePlus} kind={'primary'} variant={'ghost'}>
    <>Primary</>
  </Button>
</Bar>

<Bar>
  <Button icon={FiFilePlus} disabled kind={'normal'} variant={'outline'}>
    <>Normal</>
  </Button>
  <Button icon={FiFilePlus} disabled kind={'danger'} variant={'outline'}>
    <>Danger</>
  </Button>
  <Button icon={FiFilePlus} disabled kind={'primary'} variant={'outline'}>
    <>Primary</>
  </Button>
</Bar>

<Bar>
  <Button icon={FiFilePlus} disabled kind={'normal'} variant={'filled'}>
    <>Normal</>
  </Button>
  <Button icon={FiFilePlus} disabled kind={'danger'} variant={'filled'}>
    <>Danger</>
  </Button>
  <Button icon={FiFilePlus} disabled kind={'primary'} variant={'filled'}>
    <>Primary</>
  </Button>
</Bar>

## Normal

<Bar>
  <Button icon={FiFilePlus} kind={'normal'} variant={'ghost'}>
    <>Normal</>
  </Button>
  <Button icon={FiFilePlus} kind={'normal'} variant={'ghost'} data-hover>
    <>Hovered</>
  </Button>
  <Button icon={FiFilePlus} kind={'normal'} variant={'ghost'} data-focus>
    <>Focused</>
  </Button>
  <Button icon={FiFilePlus} kind={'normal'} variant={'ghost'} data-active>
    <>Active</>
  </Button>
</Bar>

<Bar>
  <Button icon={FiFilePlus} kind={'normal'} variant={'outline'}>
    <>Normal</>
  </Button>
  <Button icon={FiFilePlus} kind={'normal'} variant={'outline'} data-hover>
    <>Hovered</>
  </Button>
  <Button icon={FiFilePlus} kind={'normal'} variant={'outline'} data-focus>
    <>Focused</>
  </Button>
  <Button icon={FiFilePlus} kind={'normal'} variant={'outline'} data-active>
    <>Active</>
  </Button>
</Bar>

<Bar>
  <Button icon={FiFilePlus} kind={'normal'} variant={'filled'}>
    <>Normal</>
  </Button>
  <Button icon={FiFilePlus} kind={'normal'} variant={'filled'} data-hover>
    <>Hovered</>
  </Button>
  <Button icon={FiFilePlus} kind={'normal'} variant={'filled'} data-focus>
    <>Focused</>
  </Button>
  <Button icon={FiFilePlus} kind={'normal'} variant={'filled'} data-active>
    <>Active</>
  </Button>
</Bar>

## Primary

<Bar>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'ghost'}>
    <>Ghost</>
  </Button>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'ghost'} data-hover>
    <>Hovered</>
  </Button>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'ghost'} data-focus>
    <>Focused</>
  </Button>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'ghost'} data-active>
    <>Active</>
  </Button>
</Bar>

<Bar>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'outline'}>
    <>Outline</>
  </Button>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'outline'} data-hover>
    <>Hovered</>
  </Button>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'outline'} data-focus>
    <>Focused</>
  </Button>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'outline'} data-active>
    <>Active</>
  </Button>
</Bar>

<Bar>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'filled'}>
    <>Outline</>
  </Button>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'filled'} data-hover>
    <>Hovered</>
  </Button>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'filled'} data-focus>
    <>Focused</>
  </Button>
  <Button icon={FiPhoneCall} kind={'primary'} variant={'filled'} data-active>
    <>Active</>
  </Button>
</Bar>

## Danger

<Bar>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'ghost'}>
    <>Ghost</>
  </Button>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'ghost'} data-hover>
    <>Hovered</>
  </Button>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'ghost'} data-focus>
    <>Focused</>
  </Button>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'ghost'} data-active>
    <>Active</>
  </Button>
</Bar>

<Bar>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'outline'}>
    <>Outline</>
  </Button>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'outline'} data-hover>
    <>Hovered</>
  </Button>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'outline'} data-focus>
    <>Focused</>
  </Button>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'outline'} data-active>
    <>Active</>
  </Button>
</Bar>

<Bar>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'filled'}>
    <>Outline</>
  </Button>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'filled'} data-hover>
    <>Hovered</>
  </Button>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'filled'} data-focus>
    <>Focused</>
  </Button>
  <Button icon={FaDeleteLeft} kind={'danger'} variant={'filled'} data-active>
    <>Active</>
  </Button>
</Bar>

## Custom styles

<Bar>
  <Button
    kind={'primary'}
    style={{
      display: 'flex',
      flexDirection: 'column',
      placeContent: 'center center',
      maxWidth: `var(--sizes-7)`,
      whiteSpace: 'normal',
    }}
  >
    <FiFolder size={'var(--sizes-6)'} strokeWidth={0.5} />
    Some long dir name
  </Button>
</Bar>
