# Menu

- Should be a grid with 3 columns:
  - Optional icon prefix
  - Text:
    - This is not a simple text. We want to support inline filter. Hence, this
      should be `DecoratedText`.
  - Other info, which should be any component:
    - Toggle state for boolean flags
    - Numeric or something else.
    - An icon to indicate submenu.
