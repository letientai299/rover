import styles from './RangerLike.module.scss';

interface RangerLikeProps {}

const RangerLike = (props: RangerLikeProps) => {
  return <div className={styles.RangerLike}>Ranger like</div>;
};

export default RangerLike;
